# Project: OBI extension

## What it does
Generates build commands for IBM i

## Conventions
- TypeScript strict mode, no `any`.
- Command IDs use the prefix `obi-`.
- Every new command/setting must be added to `package.json`.